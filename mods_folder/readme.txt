currently still in development

i using polymod to make this engine have a modding system!

current having:
- data folder (can contains song data .json file)
- images folder (can contains characters and icon file)
- songs folder (can contains songs .ogg file)